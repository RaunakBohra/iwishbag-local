#!/bin/bash

# Claude MCP Quick Launcher
# This script loads environment variables and launches Claude with MCP servers

# Load environment variables
if [ -f .env.mcp ]; then
    set -a
    source .env.mcp
    set +a
fi

# Launch Claude with the current directory
exec claude "$@"