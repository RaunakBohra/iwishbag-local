# Cloudflare Pages _redirects file
# Handle client-side routing for React SPA

# Redirect all routes to index.html for client-side routing
/*    /index.html   200

# Optional: Add any specific redirects here
# /old-path  /new-path  301